build
.idea
cmake-build-debug
duckdb_unittest_tempdir/
.DS_Store
testext
test/python/__pycache__/
.Rhistory
# Anchor to the repo root so this only ignores a top-level data/ dir (large/benchmark
# data), NOT test/data/ — test fixtures live there and must be committed for CI.
/data/
*.parquet
.claude/*local*
.superset/
Note.md
.cache/
# vcpkg manifest-mode local install output (build artifact)
/vcpkg_installed/
# Vendored flatbuffers + flatcitybuf sources and install prefix (`just vendor-fcb`)
.vendor/
# Node smoke harness for the wasm build (`just test-wasm`): installed deps, and the
# scratch HOME the harness points DuckDB-Wasm's extension cache at.
test/wasm/node_modules/
test/wasm/.cache-home/
